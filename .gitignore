# Python
__pycache__/
*.pyc

# Generated data (outputs, may contain site data)
data/*.json
# ...but keep the committed sample data so the pipeline runs out of the box
!data/*.example.json

# Optimized page outputs
final output/*.html

# Source exports (site data, keep out of the repo)
*.xlsx
gsc/

# OS
.DS_Store
Thumbs.db
